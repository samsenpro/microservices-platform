# Perfil "test": sin Config Server (ver surefire en el pom raíz); se replica lo imprescindible de
# config-repo/service-discovery.yml.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

spring:
  security:
    user:
      name: eureka-test
      password: eureka-test-password
